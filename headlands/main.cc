#include <iostream>

/*
Given a csv file, each row being (start_job, time, next_job), 
Your job is to detect if the input data is an union of single linked list; 
then compute the total time to finish all jobs, the total number of jobs, average time per job. 
Read the csv file, run an algorithm, writing output.
*/

int main() {
    std::cout << "test" << std::endl;
}